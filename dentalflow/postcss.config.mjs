/**
 * PostCSS Configuration for Tailwind CSS v4
 *
 * Uses the new @tailwindcss/postcss package for v4 compatibility
 */

const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}

export default config
