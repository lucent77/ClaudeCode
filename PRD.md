# 📄 DentalFlow - 요구사항 정의서 (PRD)

> **문서 버전**: 1.1
> **작성일**: 2025-11-29
> **최종 수정**: 2025-11-29
> **프로젝트명**: DentalFlow (치과 기공소 CADCAM 작업 관리 시스템)

---

## 1. 프로젝트 개요

**한 줄 요약**: 치과 기공소의 CADCAM 작업을 부서별로 실시간 추적하고, 관리자가 전체 현황을 한눈에 파악할 수 있는 웹 기반 작업 관리 시스템

---

## 2. 핵심 타겟 (페르소나)

| 구분 | 사용자 유형 | 설명 |
|------|------------|------|
| 👷 | **작업자** | 9개 부서(Front Desk, PreCAD, Solidex Design, Cocr Design, 3D Print Design, PreCAM, Solidex, Cocr, 3D Print) 소속 직원. 자신의 부서에 할당된 작업만 확인하고 처리 |
| 👨‍💼 | **관리자** | 전체 작업 현황 모니터링, 작업 할당, 통계 대시보드 조회 |

**예상 사용자 규모**: 80명 (사내용)

---

## 3. 주요 기능 (MVP Scope)

### 3.1 필수 기능

#### ✅ 기능 1: 역할 기반 로그인 및 부서별 작업 뷰
- 사용자는 로그인 시 본인 부서의 작업 목록만 확인
- 역할: `admin` (관리자), `worker` (작업자)
- 부서 배정은 관리자가 사용자 등록 시 설정

#### ✅ 기능 2: 케이스(작업) 관리 시스템
- **케이스 정보 필드**:
  - `CASE NUMBER` (고유번호, 자동생성)
  - `입력시간` (자동기록)
  - `Due Date` (마감일)
  - `Pan Number`
  - `LAB/DOCTOR 이름`
  - `환자명`
  - `개수`
  - `치아 번호`
  - `치아색`
  - `임플란트 종류`
  - `NOTE` (다중 선택 가능한 옵션 + 자유 텍스트)

- **작업 유형 필드** (해당 부서만 표시):
  - `Cocr Type`
  - `Solidex Type`
  - `3D Print Type`

- **작업 상태**: `대기중`, `진행중`, `완료`, `보류`, `취소`

#### ✅ 기능 3: 실시간 다중 사용자 동시 작업
- 여러 사용자가 동시에 작업 상태를 변경해도 충돌 없이 반영
- 다른 사용자의 변경 사항이 화면에 실시간으로 업데이트
- 낙관적 잠금(Optimistic Locking)으로 데이터 무결성 보장

#### ✅ 기능 4: 관리자 대시보드
- **실시간 모니터링**: 전체 케이스 현황, 부서별 진행 상황
- **통계 지표**:
  - 부서별 작업량 (일별/주별)
  - 완료율
  - 지연 건수 및 리스트 (Due Date 초과)
  - 작업자별 작업 통계

#### ✅ 기능 5: 작업 흐름 관리
- 케이스가 부서 간 순차적으로 이동 (일부 과정 스킵 가능)
- 주요 생산 부서: Solidex, Cocr, 3D Print
- 케이스별로 거쳐야 할 부서를 선택적으로 지정 가능

#### ✅ 기능 6: 부서별 작업 커스터마이징 (신규)
- **부서별 커스텀 작업 유형 관리**:
  - 각 부서가 자신만의 작업 유형(Task Type)을 정의 가능
  - 예: Solidex 부서 → "풀 크라운", "인레이", "온레이", "브릿지" 등
  - 예: 3D Print 부서 → "모델", "서지컬 가이드", "스플린트" 등

- **부서별 커스텀 필드 설정**:
  - 부서 특성에 맞는 추가 입력 필드 정의
  - 필드 타입: 텍스트, 숫자, 선택(드롭다운), 다중선택, 날짜
  - 필수/선택 여부 설정 가능

- **부서별 상태 커스터마이징**:
  - 기본 상태(대기중, 진행중, 완료, 보류, 취소) 외 추가 상태 정의
  - 예: "검수중", "수정요청", "외주진행" 등

- **관리자 설정 화면**:
  - 부서별 작업 유형 CRUD
  - 커스텀 필드 CRUD
  - 드래그 앤 드롭으로 순서 변경

#### ✅ 기능 7: 작업자별 업무 할당 및 마이 태스크 (신규)
- **개별 작업자 할당**:
  - 케이스 내 각 부서 작업을 특정 작업자에게 할당
  - 한 케이스에 부서별로 다른 작업자 지정 가능
  - 작업자 미지정 시 해당 부서 전체에 표시

- **마이 태스크 (개인 업무 대시보드)**:
  - 로그인 시 기본 화면: 본인에게 할당된 작업만 표시
  - **오늘의 할 일**: 오늘 마감 또는 긴급 작업
  - **진행 중인 작업**: 현재 담당 중인 케이스
  - **대기 중인 작업**: 아직 시작하지 않은 할당 작업
  - **완료한 작업**: 최근 완료 이력 (7일간)

- **작업 가져오기 (Self-Assign)**:
  - 미할당 작업 목록에서 본인이 직접 작업 선택 가능
  - "내가 할게요" 버튼으로 즉시 본인에게 할당

- **작업 인계 (Transfer)**:
  - 담당 작업을 같은 부서 다른 작업자에게 인계
  - 인계 사유 입력 필수
  - 인계 이력 자동 기록

- **작업자별 통계**:
  - 일별/주별/월별 완료 건수
  - 평균 처리 시간
  - 지연율

---

### 3.2 추후 고도화 기능 (MVP 제외)

| 기능 | 설명 |
|------|------|
| 파일 첨부 | CAD 파일, 이미지 첨부 기능 |
| 알림 시스템 | 마감 임박, 작업 할당 알림 (이메일/푸시) |
| 보고서 출력 | PDF/Excel 내보내기 |
| 기존 시스템 연동 | Google Sheets 데이터 마이그레이션 도구 |
| 모바일 최적화 | 반응형을 넘어선 전용 모바일 뷰 |

---

## 4. 사용자 흐름 (User Flow)

### 4.1 작업자 흐름

```
[로그인] → [부서별 작업 목록 확인] → [작업 상태 변경] → [완료]
    │              │                        │
    │              ▼                        ▼
    │         필터/검색 기능          실시간 동기화
    │         (CASE NUMBER,           (다른 사용자에게
    │          환자명, 상태)           즉시 반영)
    ▼
  부서 자동 인식
```

### 4.2 관리자 흐름

```
[로그인] → [대시보드 조회] → [케이스 생성/수정] → [모니터링]
    │           │                   │               │
    │           ▼                   ▼               ▼
    │      통계 차트             작업 할당       지연 건
    │      (부서별, 기간별)      (부서 지정)     알림 확인
    ▼
  전체 권한 접근
```

### 4.3 케이스 생명주기

```
[Front Desk 접수]
       │
       ▼
[PreCAD] ──────────────────────────────────┐
       │                                   │
       ▼                                   │ (스킵 가능)
[Design 단계] ─┬─ Solidex Design           │
              ├─ Cocr Design               │
              └─ 3D Print Design           │
       │                                   │
       ▼                                   │
[PreCAM] ◄─────────────────────────────────┘
       │
       ▼
[Production] ─┬─ Solidex (주요)
              ├─ Cocr (주요)
              └─ 3D Print (주요)
       │
       ▼
   [완료]
```

---

## 5. 비기능적 요구사항

| 항목 | 요구사항 |
|------|---------|
| **동시 사용자** | 최소 80명 동시 접속 지원 |
| **응답 시간** | 페이지 로딩 2초 이내 |
| **실시간성** | 데이터 변경 시 1초 이내 타 사용자 화면 반영 |
| **가용성** | 업무 시간 중 99% 이상 가동률 |
| **호스팅** | Hostinger VPS 환경 |

---

## 6. 화면 구성 (와이어프레임 가이드)

### 6.1 로그인 페이지
- 이메일/비밀번호 입력
- "비밀번호 찾기" 링크

### 6.2 작업자 메인 화면
- 상단: 사용자 정보, 소속 부서 표시
- 중앙: 작업 목록 테이블 (필터, 검색, 정렬)
- 각 행: 케이스 정보 + 상태 변경 버튼

### 6.3 관리자 대시보드
- 상단: 핵심 KPI 카드 (총 케이스, 진행중, 지연, 완료율)
- 중앙: 부서별 작업 현황 차트
- 하단: 전체 케이스 목록 (필터, 검색, 일괄 작업)

### 6.4 케이스 상세/수정 모달
- 모든 필드 표시
- 작업 이력 타임라인

### 6.5 마이 태스크 화면 (신규)
- 상단: 오늘의 요약 카드 (오늘 할 일, 진행중, 완료)
- 좌측: 할당된 작업 목록 (우선순위순)
- 우측: 선택한 작업 상세 정보
- 하단: 최근 완료 이력

### 6.6 부서 설정 화면 (관리자 전용, 신규)
- 탭: 작업 유형 | 커스텀 필드 | 상태 관리
- 각 탭: 항목 목록 + 추가/수정/삭제 버튼
- 드래그 앤 드롭 순서 변경

### 6.7 작업자 할당 모달 (신규)
- 케이스 상세에서 "작업자 할당" 버튼 클릭 시
- 부서별 작업자 목록 표시
- 현재 작업량 표시 (바쁨 정도 시각화)
- 할당/인계 버튼

---

## 7. 용어 정의

| 용어 | 설명 |
|------|------|
| Case | 하나의 치과 보철물 제작 주문 단위 |
| Pan Number | 작업 트레이 식별 번호 |
| Solidex | 도자기(세라믹) 보철물 제작 |
| Cocr | 코발트-크롬 합금 보철물 제작 |
| 3D Print | 3D 프린팅 기반 보철물 제작 |

---

*이 문서는 MVP(최소 기능 제품) 범위만을 정의합니다. 추가 기능은 1차 출시 이후 고도화 단계에서 진행합니다.*
